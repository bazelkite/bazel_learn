# This project has been migrated to use MODULE.bazel (Bzlmod)
# This WORKSPACE file is kept for backwards compatibility only
# All dependencies are now managed in MODULE.bazel

workspace(name = "bazel_training_example")
